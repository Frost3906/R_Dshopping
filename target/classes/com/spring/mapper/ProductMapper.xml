<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.mapper.ProductMapper">

	<select id="searchList" resultType="com.spring.domain.ProductVO">
		select * 
		from product
		where p_code in
		(select p_code 
		from searchList 
		where p_name like upper('%'||#{keyword}||'%') or
			  p_code like '%'||#{keyword}||'%' or 
			  p_content like upper('%'||#{keyword}||'%') or
			  p_category1 like upper('%'||#{keyword}||'%') or
			  p_category2 like upper('%'||#{keyword}||'%') or
			  p_category3 like upper('%'||#{keyword}||'%'))
	</select>
	
	<select id="getProduct" resultType="com.spring.domain.ProductVO">
		select * from product where p_code = #{code}
	</select>

</mapper>